# Shared runtime dependencies. Install the platform file instead of this one:
#   Windows / Linux / Intel Mac:  pip install -r requirements-win.txt
#   Apple Silicon Mac:            see requirements-mac.txt
PySide6==6.11.2  # full package: QtMultimedia lives in PySide6-Addons
av>=14
numpy>=1.26
python-docx>=1.1
certifi
