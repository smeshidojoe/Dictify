# Optional, Windows with an NVIDIA GPU: cuBLAS for faster-whisper (CTranslate2 needs nothing else).
# The app itself downloads the same DLLs on demand, see dictify/gpu.py.
nvidia-cublas-cu12==12.9.1.4
